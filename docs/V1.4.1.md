# 本命猫 V1.4.1 热修任务书(Codex 版)

> **给操作者(人类)的使用说明**:将本文件放入仓库 `docs/V1.4.1.md`,对 Codex 下达以下一条指令:
>
> ```
> 完整阅读 docs/V1.4.1.md,按任务组 H1→H2→H3→H4→H5→H6→H7→H8 顺序执行全部任务。附录 S-1 的脚本代码与附录全部文案必须逐字使用,禁止改写。全部完成后运行 R 组回归验证,把 npm run report:similarity 的完整输出原文粘贴给我。
> ```

---

## 第〇部分:执行者守则(最高优先级)

沿用 V1.4 守则 1~6(禁接LLM/禁自创文案/禁新增依赖/禁改题面/禁改支付模型/每组过测试),并新增:

7. **禁止修改验收机制**:`scripts/similarity-check.mjs` 必须整体替换为附录 S-1 的代码原文;禁止改动其中的随机种子机制、对数(10对)、阈值(均值≤35%、最差≤42%);禁止修改 `lib/report-similarity.ts` 的剔除规则(reasonableRepeat 相关逻辑)。若认为规则有误,在完成报告中提出,不得先斩后奏。
8. **完成报告必须附证据**:粘贴 `npm run report:similarity` 与 `npm run distribution:check` 的完整输出原文。

---

## 第一部分:V1.4 复盘(为什么要热修,30秒读完)

V1.4 的反模板工程质量是好的(织入、对比块、判词轮换全部生效),但核心验收 F1 是假通过:旧版 similarity-check.mjs 把两个 sessionId **写死**,恰好抽中一对幸运变体组合(29.2%)。用随机种子重测 24 对:验收口径均值 44%~49%,最差 63%,无一对达标。

根因:变体池只覆盖了 7 个开场/过渡句位,而重复的大头在**正文**——风险段、情绪需求段、固定过渡句、单变体织入模板、同 flag 建议行。本次热修=把这些正文句位全部纳入变体池(文案见附录),并把验收脚本换成不可作弊的随机多对版本。

---

## 任务组 H1 — 验收脚本替换(先做,后续每步都用它自测)

用附录 S-1 的代码**整体替换** `scripts/similarity-check.mjs`。行为:10 对随机 sessionId、固定答案组、计算验收口径重复率的均值与最差值、聚合输出跨对"撞车榜"(重复次数最多的句子排名)。`package.json` 的 `report:similarity` 命令保持不变。

替换后先跑一次,记录热修前基线。

---

## 任务组 H2 — 正文变体池扩容(核心工程)

以下每个句位改为从变体池按种子选取。**种子规则**:`hash(sessionId + 句位key)`——同一用户刷新永远同一版本;不同句位之间相互独立(禁止用同一个哈希值给所有句位取模,那会导致所有句位同时撞车或同时错开)。

挂载点与文案对应关系:

| 句位 | 现状 | 文案 |
|---|---|---|
| 贴标签过渡句(人格段末尾) | 固定1句 | 附录 T-1(3变体) |
| 关注句(情绪需求段末尾) | 固定1句 | 附录 T-2(3变体) |
| 情绪需求段(5档) | 每档固定1段 | 附录 T-3(每档3变体,整段替换,含原有"循序渐进"等尾句一并删除,信息已并入变体) |
| 风险段(成本/清洁/匹配/突发) | 每型固定1条 | 附录 T-4(每型3变体;序号"1./2./3."由模板补,不在文案内) |
| 综合判断·无冲突句 | 固定1句 | 附录 T-5(3变体) |
| V1.4 已有的 7 个引入句位 | 每位3变体 | 附录 T-6(每位追加第4变体) |

若代码中存在附录未覆盖的风险类型或其他固定正文句,**保持原文不动**并在完成报告中列出清单,不得自行写变体。

---

## 任务组 H3 — 织入模板修缮

**H3-1 变体补齐**:按附录 T-7 把各类别织入模板补至 3 个变体(consent 现仅 1 个,是同答案闺蜜必然逐字撞车的直接原因)。模板选取同样按 `hash(sessionId + 类别key)` 种子化。

**H3-2 类别-段落匹配**(修复"医疗引用出现在人格段"的突兀感):
- 人格段织入:只从 {作息、陪伴、掉毛} 类别中选
- "为什么是它"段织入:只从 {陪伴、作息} 中选
- 现实适配度段织入:只从 {医疗、预算、许可、空间} 中选
- 某段落无可用类别信号时,**跳过该织入位**,不得跨类错挂。

---

## 任务组 H4 — 建议行变体

现实适配度的建议行(预算/空间/作息/医疗承受力)当两位用户 flag 相同时逐字重复。为附录 T-8 列出的 9 条建议行各挂载第 2 变体,按种子选取。挂载方式:按语义对应到现有分支(附录标注了 flag 含义);找不到完全对应分支的条目,列入完成报告的 TODO 清单,不得自创或就近硬塞。

---

## 任务组 H5 — 对比块修缮

1. **零信息维度跳过**:某维度三只品种取值完全相同时(如"小空间适配:高/高/高"),该维度不输出,顺延选取下一个有差异的维度;凑不满 3 个有差异维度时,输出 2 个即可。
2. **结论句连词**:约束维度之间用「、」连接,且最多写 2 个维度(现状"陪伴时间和空间和预算"三连"和"废弃)。结论句模板改为:「一句话:如果{维度A}、{维度B}是你的硬约束,{低压选项}会让你们都轻松;如果你确定能扛,{主推}值得。」
3. **备选段排版**:两只备选品种各自独立成段(现状挤在同一段无换行)。

---

## 任务组 H6 — 字符串修正(逐字替换)

1. reportVariants.json 中「聪明的决定都有 Plan B,你的在这里。」→「聪明的决定都有 Plan B——你的在这里。」
2. 标点归一化器必须同样作用于变体池与织入模板输出(本次半角逗号即从变体字符串漏网)。
3. 全库扫描所有 content/*.json 与报告模板字符串:中文语境下不得残留半角 `,` `:` `;`(代码与 URL 除外)。

---

## 任务组 H7 — V1.4 遗留验证(逐项给出证据,未完成则补齐)

- **H7-1** PAY_URL 缺失时,付费墙实际渲染 X-1 兜底文案(「购买入口暂时维护中……」含客服出口),不得出现无出口的"回到购买渠道"旧文案。
- **H7-2** 静海守灯人 altBreeds 含「英短蓝猫」且理由为 C-2 原文;breeds.json 存在英短蓝猫条目。
- **H7-3** 破晓远征家备选孟加拉豹猫理由为 C-3 原文(含"精力过剩"门槛提示)。
- **H7-4** 付费墙勾选清单含第 5 条「✓ 解锁后还有一步小定制:这份报告连语气都由你决定」。
- **H7-5** 微反馈为独立过场页(第4/8/12题后),非 toast;分享卡含猫元素且卡底 URL 读取 NEXT_PUBLIC_SITE_URL;结果页分步揭晓动效存在。已完成则各给一行证据(文件+行号或截图说明)。

---

## 任务组 H8 — 共享码轮换宽限期

背景:共享码现于北京时间 18:00 立即轮换,旧码瞬间失效。用户 17:50 取码答题、18:05 提交核销会收到通用报错「兑换码无效或已使用」,误以为买到假码。

1. `verifySharedAccessCode`:校验当前窗口码失败时,追加校验**上一窗口**的码;若命中且当前时间距上一窗口结束 ≤15 分钟,视为有效(返回上一窗口的 SharedAccessCode)。小时窗(60分钟)模式同样适用 15 分钟宽限。
2. `/api/redeem`:能够区分"格式合法但已过宽限期的共享码"(用上一窗口码比对即可识别),此时返回专属报错:「该限时码已更换(每天 18:00 轮换),请到获取渠道领取最新码。」其余无效码保持原报错不变。
3. 新增单测:①17:59 取码、18:10 核销 → 成功;②18:20 核销上一窗口码 → 返回专属报错;③18:20 核销随机乱码 → 返回原通用报错;④宽限期内的旧码与当前新码同时有效。

---

## 任务组 R — 回归验收(逐项输出通过/失败)

- **R1(核心)** `npm run report:similarity`:10 对随机种子,均值 ≤35% 且最差 ≤42%。粘贴完整输出。若未达标:按脚本输出的撞车榜,向榜首句位追加第 4 变体的**申请**(列出句位名),等待人类提供文案后再继续——不得自写。
- **R2** 同一 sessionId 重复生成报告,内容逐字节一致(种子稳定性)。
- **R3** 织入类别-段落匹配:人格段不出现医疗/预算/许可类引用;各段织入正常。
- **R4** 对比块无全同取值维度;结论句用顿号且最多2维度;备选两段分离。
- **R5** 全库无中文语境半角标点;Plan B 句已修正。
- **R6** H7 五项证据齐全。
- **R7** `npm run distribution:check` 比值 ≤2.5;`npx vitest run` 全绿;`npm run typecheck` 通过。
- **R8** 共享码宽限期四条单测全绿;18:00 边界前后各核销一次的手动模拟通过,专属报错文案逐字一致。

---
---

# 附录

## S-1 scripts/similarity-check.mjs(整体替换,逐字使用)

```js
import { breedByName, personas, questions } from "../lib/content";
import { buildLocalReport } from "../lib/local-report";
import { collectPremiumFlags } from "../lib/premium";
import { reportSentenceSimilarity } from "../lib/report-similarity";
import { detectBreedConflict, scoreAnswers } from "../lib/scoring";
import { randomUUID } from "node:crypto";

const ANSWERS = [
  [0, 3, 0, 1, 1, 1, 2, 1, 0, 0, 3, 1, 3, 3, 2, 3],
  [2, 3, 0, 3, 0, 2, 2, 2, 0, 0, 3, 3, 1, 3, 2, 3],
];
const PREMIUM_ANSWERS = [
  [1, 1, 1, 1, 0, 0, 1],
  [2, 0, 2, 0, 0, 1, 2],
];
const PAIRS = 10;
const MEAN_LIMIT = 0.35;
const MAX_LIMIT = 0.42;

function createReport(sessionId, answers, premiumAnswers) {
  const score = scoreAnswers(answers);
  const persona = personas.find((item) => item.id === score.personaId);
  if (!persona) throw new Error(`找不到人格: ${score.personaId}`);
  const report = buildLocalReport({
    sessionId,
    persona,
    answersSummary: answers.map((optionIndex, index) => (
      `${questions[index].text} → ${questions[index].options[optionIndex].text}`
    )),
    hardFlags: score.hardFlags,
    premiumFlags: collectPremiumFlags(premiumAnswers),
    breedFacts: breedByName(persona.primaryBreed.name),
    conflict: detectBreedConflict(persona, score.hardFlags),
  });
  return { report, persona };
}

const ratios = [];
const repeatCounter = new Map();
for (let i = 0; i < PAIRS; i += 1) {
  const a = createReport(randomUUID(), ANSWERS[0], PREMIUM_ANSWERS[0]);
  const b = createReport(randomUUID(), ANSWERS[1], PREMIUM_ANSWERS[1]);
  if (a.persona.id !== "dusk_weaver" || b.persona.id !== "dusk_weaver") {
    throw new Error(`F1 人格偏移: ${a.persona.id}, ${b.persona.id}`);
  }
  const result = reportSentenceSimilarity(a.report, b.report, {
    reasonableRepeatLines: [a.persona.signatureParagraph, a.persona.primaryBreed.reason],
    reasonableRepeatPrefixes: a.persona.altBreeds.map((item) => `${item.name}：`),
  });
  ratios.push(result.variableRatio);
  for (const sentence of result.variableDuplicates) {
    repeatCounter.set(sentence, (repeatCounter.get(sentence) ?? 0) + 1);
  }
}

const mean = ratios.reduce((sum, x) => sum + x, 0) / ratios.length;
const max = Math.max(...ratios);
console.log(`各对验收重复率: ${ratios.map((x) => `${(x * 100).toFixed(0)}%`).join(" ")}`);
console.log(`均值 ${(mean * 100).toFixed(1)}%(阈值 ≤${MEAN_LIMIT * 100}%) 最差 ${(max * 100).toFixed(1)}%(阈值 ≤${MAX_LIMIT * 100}%)`);
console.log("撞车榜(跨10对重复次数最多的叙述句):");
const board = [...repeatCounter.entries()].sort((x, y) => y[1] - x[1]).slice(0, 10);
for (const [sentence, count] of board) {
  console.log(`  ${count}次 | ${sentence}`);
}
if (mean > MEAN_LIMIT || max > MAX_LIMIT) {
  console.error("未达标:请按撞车榜定位仍在重复的句位");
  process.exitCode = 1;
}
```

## T-1 贴标签过渡句(3变体)

1. 「这些不是给你贴标签,而是说明你在亲密关系里既需要温度,也需要边界和节奏——一只合适的猫,恰好懂得怎么和这样的你相处。」
2. 「以上都不是评价,是坐标——它们标出了你在一段关系里的舒适区,而选猫,本质上就是选一段关系。」
3. 「把这些放在一起看,不是为了定义你,是为了找到那只天生和你同频的猫。」

## T-2 关注句(3变体)

1. 「你最该关注的是猫的活动量、粘人度和护理成本,它们比单纯品种名更能决定日后是否舒服。」
2. 「比起品种名,真正决定日常幸福感的是三件事:它多能闹、多粘人、多难打理。」
3. 「挑猫盯住三个指标就够了:活动量、粘人度、护理成本——名字好听不能当饭吃。」

## T-3 情绪需求段(5档×3变体,整段替换)

**companionship(稳定陪伴)**
1. 「你最需要的是稳定陪伴:家里有一个生命在自己的节奏里靠近你、等待你。它不必时刻黏着你,'在'本身就是意义。」
2. 「你要的其实很简单:回家时屋里有另一个呼吸声。它不一定迎接你,但它在——这种安静的'在',比很多话都管用。」
3. 「稳定,是你在关系里最看重的东西。一只按自己节奏靠近你的猫,提供的恰好是最不吵闹的稳定。」

**comfort(被安慰)**
1. 「你真正想获得的是被安慰的感觉。猫对你来说不是摆设,而是低谷时能让房间重新有温度的存在。」
2. 「你想要的是一个不问'怎么了'也能让你好受一点的存在。猫不会安慰人,但它会把身体靠过来——很多时候这就够了。」
3. 「低谷的时候,语言经常是多余的。一只趴在旁边打呼噜的猫,是房间里最诚实的安慰。」

**routine(生活秩序)**
1. 「你想要的是一种生活秩序。照顾猫会让你每天有固定动作:添水、清砂、陪玩、观察状态。」
2. 「你需要的是让日子长出骨架。固定的添水、清砂、陪玩,会把散掉的生活重新钉回节奏里。」
3. 「照顾一只猫的本质是照顾秩序——而秩序,恰好是你现在最想抓住的东西。」

**playfulness(互动快乐)**
1. 「你想要的是家里常驻一个快乐源泉:逗它、看它疯、被它气笑,无聊从此没有立足之地。」
2. 「对你来说猫不是摆件,是玩伴——能接住你的兴致,也能主动挑起战争的那种。」
3. 「你需要的是每天有一场不用出门的即兴演出,而一只爱闹的猫,场场不迟到。」

**uncertain(还说不清)**
1. 「你还说不清想从养猫里得到什么——这不是问题,很多好关系都是先开始,再懂得。」
2. 「答案没想清楚也没关系。把条件准备扎实,让相处替你回答。」
3. 「说不清'为什么想养',往往说明这份念头来自更深的地方。不急,先把功课做好。」

## T-4 风险段(4型×3变体;序号由模板补)

**成本风险**
1. 「成本风险:长期支出会比第一眼看到的用品价格更重要。解决方案:先做月预算和突发医疗预算,把数字写下来再决定。」
2. 「成本风险:养猫是订阅制,不是买断制——月月都有账单。解决方案:把月预算写成固定支出,医疗备用金单独立户,不动它。」
3. 「成本风险:第一年的花费通常是预想的一点五倍。解决方案:按清单先算一遍总账,再定接猫时间,别让惊喜变惊吓。」

**清洁风险**
1. 「清洁风险:掉毛、猫砂和气味会持续出现。解决方案:固定猫砂盆位置,选低粉尘猫砂,准备滚毛器和每周清洁节奏。」
2. 「清洁风险:毛会出现在你意想不到的地方,包括刚洗好的衣服上。解决方案:滚毛器常备三个,猫砂盆远离卧室,每周一次大清。」
3. 「清洁风险:换毛季的清洁量会翻倍。解决方案:提前定好'每天五分钟、每周一小时'的节奏,比事后崩溃便宜。」

**匹配风险**
1. 「匹配风险:即使结论很顺,也不要把品种当保证。解决方案:看猫时观察亲人度、胆量、食欲和应激反应,不要只看照片。」
2. 「匹配风险:品种描述是平均值,你接回来的是个体。解决方案:接触时看它敢不敢吃东西、愿不愿靠近人——状态比血统诚实。」
3. 「匹配风险:再合适的品种,也可能遇到不合拍的那一只。解决方案:第一面多待半小时,观察胆量和食欲,别为照片下单。」

**突发风险**
1. 「突发风险:医疗备用金不足时,一次就诊就会变成压力。解决方案:先存一笔专款,没有存够前先继续做功课。」
2. 「突发风险:猫不会提前请假生病。解决方案:留出一笔不挪用的就诊专款,把最近的二十四小时医院存进备忘录。」
3. 「突发风险:一次急诊可能等于三个月的预算。解决方案:备用金没到位之前,把'再等等'当成负责,不是拖延。」

## T-5 综合判断·无冲突句(3变体)

1. 「综合判断:目前没有明显硬冲突,但仍要记住,品种只是参考,真实相处还要看具体猫的性格。」
2. 「综合判断:硬条件这关你过了——剩下的功课只有一件:去见具体的那一只,性格对上才算数。」
3. 「综合判断:没有需要拦你的硬冲突。接下来把注意力放在'个体'上:同品种的两只猫,性格可以差出一整个星座。」

## T-6 已有引入句位的第4变体(各追加1条)

- opening.gentle:「谢谢你认真答完了每一题——接下来换这份报告认真地回应你。」
- opening.direct:「规则只有一条:不糊弄。合适就说合适,该补课就说补课。」
- opening.practical:「这份报告的每一段都朝一个目标去:让你今天就知道下一步做什么。」
- opening.emotional:「想养一只猫,常常是心里某个角落先开了口——我们先听听它想说什么。」
- whyBreed:「不是我们把{breed}推给你,是你的答案一路把它领到了你面前。」
- realityIntro:「浪漫到此告一段落,下面这几行,是把'喜欢'过成'日子'的说明书。」
- emotionIntro:「选猫之前,先诚实地回答:你希望它填上生活里的哪一块?」
- riskIntro:「先把三种最贵的教训写在前面,你就不用亲自交学费了。」
- altIntro:「世界上不止一只猫适合你——这是好消息,不是敷衍。」
- checklistIntro:「别背,收藏就行——接猫前一周,拿出来逐项打勾。」

## T-7 织入模板补齐({answer}=选项原文,{breed}=主推品种)

**consent(补至3)**
2. 「你说{answer}——所以这份报告把'先拿到点头'排在所有建议前面。」
3. 「关于同住人的态度,你的回答是{answer}。这件事定了,后面的一切才算数。」

**各类别追加第3变体**
- schedule:「{answer}——猫是活在规律里的动物,你的这份节奏,它会喜欢。」
- shedding:「掉毛这题你选了『{answer}』,方案里的品种和工具都照这条来配。」
- care_time:「你说{answer},所以下面推荐的相处方式,都不会勉强你。」
- medical:「『{answer}』——这个答案决定了报告里'医疗'部分的分量,我们认真写了。」
- space:「{answer},空间的每一条建议都从这个前提出发。」
- budget:「你把预算写成『{answer}』,那清单里就不会出现让你肉疼的东西。」

## T-8 建议行第2变体(按语义挂载到现有分支)

1. 空间·条件稳定:「空间:居住条件稳,给它划一块固定的'自留地'——猫爬架加休息窝,安全感就有了。」
2. 空间·偏紧:「空间:面积有限不是硬伤,垂直空间是解法——一面墙的爬架,顶得上半间房。」
3. 作息·规律:「作息:你的时间表很稳,喂食和陪玩固定到点,它两周就能把你摸透。」
4. 作息·自由/独立:「作息:节奏偏自由也没关系,底线是喂食、清砂不跳票——猫要的是'可预期',不是'时刻在'。」
5. 医疗·有准备:「医疗承受力:这块你有准备,保持年检和疫苗节奏即可,不用焦虑。」
6. 医疗·吃力:「医疗承受力:先补的是备用金——体检、疫苗、驱虫加一次急诊的钱单独放好,再谈接猫。」
7. 预算·可计划:「预算:在可计划区间,原则是主粮和医疗不省,玩具和颜值项随缘。」
8. 预算·偏紧:「预算:偏紧不代表不能养,代表要把每一笔花在刀刃上——先养账本,再养猫。」
9. 预算·宽裕:「预算:压力不大,反而要防'装备党'陷阱——钱依然优先给主粮、体检和备用金。」
