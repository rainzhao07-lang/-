# 本命猫 V1.5 完成报告

日期：2026-07-13（北京时间）

## 结论

V1.5 的 A-E 与 R1-R7 已完成。生产数据层为 Supabase，新增共享码核销记录表、客服查码/批次导出、北京时间运营统计、共享码窗口限额和生产健康检查；正式站完整付费链路与限额恢复均通过。

- 正式网址：`https://benmingmao-h5.netlify.app`
- V1.5 实现提交：`ff425acfa1b7c45c26bff85397a0bdb452990991`
- Netlify 恢复不限后的部署：`6a5408711b9df1aa434cbeea`，状态 `ready`
- 分享卡实图：`E:\Downloads\本命猫V1.5验证\share-card.png`

## R1 前置门槛

```text
git log -1 --oneline
096bcf3d feat: complete V1.4.1 hotfix

各对验收重复率: 17% 9% 16% 26% 13% 9% 13% 17% 21% 8%
均值 14.8%(阈值 ≤35%) 最差 26.1%(阈值 ≤42%)

Test Files  7 passed (7)
Tests       58 passed (58)

npm run typecheck
通过

最高 15.83% / 最低 7.04% = 比值 2.25
达标(≤ 2.5)
```

## A 组与环境

生产健康检查原始响应：

```json
{
  "storage": "supabase",
  "sharedCodeConfigured": true,
  "payUrlConfigured": true,
  "siteUrlConfigured": true
}
```

生产变量清单（值已打码）：

```text
SUPABASE_URL                  已配置(all)
SUPABASE_SERVICE_ROLE_KEY     已配置(production)
ADMIN_SECRET                 已配置(production)
SHARED_ACCESS_CODE_SECRET    已配置(production)
SHARED_ACCESS_WINDOW_MINUTES 1440(production)
SHARED_DAILY_LIMIT           未配置，表示不限
NEXT_PUBLIC_PAY_URL          已配置(all)
NEXT_PUBLIC_SITE_URL         https://benmingmao-h5.netlify.app(production)
```

本地未配置 Supabase 的生产构建会输出指定保险丝：

```text
[db] 生产环境未配置 Supabase,正在使用内存兜底——严禁上线!
```

生产 health 已确认实际运行存储为 `supabase`，不存在内存兜底。

## B 组生产样例

B1 查单码（码和 session 已脱敏）：

```json
{
  "found": true,
  "code": "CZ5K****Q6",
  "used": true,
  "usedBySession": "92ffd630…",
  "usedAt": "2026-07-12T21:23:41.478308+00:00",
  "channel": "smoke",
  "batch": "smoke",
  "createdAt": "2026-07-12T21:23:38.693988+00:00"
}
```

B2 批次 JSON 导出：

```json
{
  "format": "json",
  "count": 1,
  "sample": {
    "code": "CZ5K****Q6",
    "used": true,
    "usedAt": "2026-07-12T21:23:41.478308+00:00",
    "channel": "smoke",
    "batch": "smoke",
    "createdAt": "2026-07-12T21:23:38.693988+00:00"
  }
}
```

B3 北京时间 7 日统计：

```json
{
  "days": [
    { "date": "2026-07-07", "sessions": 0, "reports": 0, "oneTimeRedeems": 0, "sharedRedeems": 0 },
    { "date": "2026-07-08", "sessions": 0, "reports": 0, "oneTimeRedeems": 0, "sharedRedeems": 0 },
    { "date": "2026-07-09", "sessions": 13, "reports": 3, "oneTimeRedeems": 3, "sharedRedeems": 0 },
    { "date": "2026-07-10", "sessions": 9, "reports": 3, "oneTimeRedeems": 0, "sharedRedeems": 0 },
    { "date": "2026-07-11", "sessions": 13, "reports": 4, "oneTimeRedeems": 1, "sharedRedeems": 0 },
    { "date": "2026-07-12", "sessions": 9, "reports": 1, "oneTimeRedeems": 0, "sharedRedeems": 0 },
    { "date": "2026-07-13", "sessions": 2, "reports": 1, "oneTimeRedeems": 1, "sharedRedeems": 1 }
  ]
}
```

CSV 导出也已验证，表头为：

```text
code,redeem_url,channel,batch,delivery_text,used,usedAt
```

## C/D 组

生产 Supabase 已执行最新 `supabase/schema.sql`，现场查询结果：

```json
{
  "table": "shared_redemptions",
  "rls": true,
  "index": "idx_shared_redemptions_window"
}
```

R5 临时限额验证：

```json
{
  "first": { "status": 200, "mode": "shared" },
  "second": {
    "status": 403,
    "error": "今天的限时名额已经用完啦——每天 18:00 会有新一批;不想等的话,购买兑换码可以立即解锁。"
  },
  "exactTaskText": true
}
```

验证后已删除临时 `SHARED_DAILY_LIMIT=1`，重新部署并用新会话复核：

```json
{ "status": 200, "mode": "shared", "limitRestored": true }
```

## R3 生产冒烟完整输出

```text
✅ health 接口可用 | status=200
✅ 存储为 supabase(严禁内存兜底) | storage=supabase
✅ PAY_URL 已配置
✅ SITE_URL 已配置
✅ 共享码密钥已配置
✅ 创建会话 | persona=暮色织梦师
✅ 结果页 200
✅ 付费入口存在(无兜底文案)
✅ 生成一次性码
✅ 一次性码核销
✅ 提交定制题
✅ 报告生成 | 1793字
✅ 报告含综合判断
✅ 报告缓存一致(种子稳定)
✅ 一码一用(复用被拒)
✅ 无效码返回400 | 兑换码无效或已使用
✅ 读取当前共享码
✅ 共享码核销

全部通过 ✅
```

## R6/R7

- 正式分享卡返回有效 PNG，文件大小 183258 字节；实图底部显示 `benmingmao-h5.netlify.app`。
- 最终本地门禁：10 个测试文件、68 项测试全部通过；`npm run typecheck` 通过。
- 50 万次分布抽样：最高 15.87%，最低 7.12%，比值 2.23，达标。
- 最终相似度：均值 17.8%，最差 34.8%，达标。
- `npm run build` 通过，17 个页面/接口路由完成构建。
