# 本命猫 V1.5 上线部署任务书(Codex 版)

> **给操作者(人类)的使用说明**:将本文件放入仓库 `docs/V1.5.md`,对 Codex 下达以下一条指令:
>
> ```
> 完整阅读 docs/V1.5.md。先核对第〇组前置门槛,不满足立即停止并报告。满足则按任务组 A→B→C→D→E 顺序执行,附录 S-2 脚本代码与全部文案逐字使用,禁止改写。完成后运行 R 组验收,把冒烟脚本完整输出、正式网址与各接口样例响应粘贴给我。
> ```
>
> 本文档的目标:从「V1.4.1 代码合格」走到「正式网址可运营」。文末附录 M 是给人类操作者的配套清单(密钥生成、发卡上架等),Codex 无需执行但需在依赖处提示等待。

---

## 第〇部分:执行者守则

沿用 V1.4 守则 1~6 与 V1.4.1 守则 7~8(禁接LLM/禁自创文案/禁新增依赖/禁改题面/禁改支付模型/每组过测试/禁改验收机制/交付附原始输出)。本轮特批两项例外:

- **特批一**:允许新增数据表 `shared_redemptions`(任务组 C 明确定义),并同步更新 `supabase/schema.sql` 与内存实现。除此之外仍禁止任何 schema 改动。
- **特批二**:允许新增只读 admin 接口(任务组 A/B 明确定义)。所有 admin 接口必须沿用现有 `x-admin-secret` 鉴权,无一例外。

## 第〇组:前置门槛(不满足则停止并报告,不得继续)

1. V1.4.1 已完成且已推送 GitHub:`git log` 可见对应提交;`npm run report:similarity` 当前即时运行达标(均值≤35%,最差≤42%)。
2. `npx vitest run`、`npm run typecheck`、`npm run distribution:check` 全部通过。
3. 操作者已提供或已在部署平台配置以下值(缺任何一项,在完成报告中列出并暂停 D 组,其余组可先做):`SUPABASE_URL`、`SUPABASE_SERVICE_ROLE_KEY`、`ADMIN_SECRET`、`SHARED_ACCESS_CODE_SECRET`、`NEXT_PUBLIC_PAY_URL`、`NEXT_PUBLIC_SITE_URL`。

---

## 任务组 A — 生产存储保险丝(本轮最重要的一条)

**背景**:部署目标为 Netlify(serverless)。`lib/db.ts` 在缺少 Supabase 配置时静默降级为内存兜底——serverless 的内存不跨请求存活,后果是:用户核销成功后,下一个请求落到另一个实例,会话未解锁、一次性码状态丢失。**内存兜底严禁出现在生产。**

**A1 健康检查接口**:新增 `GET /api/admin/health`(x-admin-secret 鉴权),返回:

```json
{ "storage": "supabase" 或 "memory", "sharedCodeConfigured": true/false, "payUrlConfigured": true/false, "siteUrlConfigured": true/false }
```

**A2 生产告警**:`NODE_ENV === "production"` 且走内存兜底时,进程启动与每次 db 初始化处输出错误日志:「[db] 生产环境未配置 Supabase,正在使用内存兜底——严禁上线!」(仅日志与 health 暴露,不改变运行行为,便于预览环境仍可跑通。)

**A3 README 上线清单**:README 增加"上线前必查"小节,列出第〇组第3条的全部环境变量,并注明:`NEXT_PUBLIC_*` 前缀变量在**构建时**固化,修改后必须重新部署才生效。

---

## 任务组 B — 客服与运营接口(全部只读,x-admin-secret 鉴权)

**B1 查单码**(客服刚需:「用户说码无效,查一下这个码」):`GET /api/admin/codes?code=XXXX` 返回:

```json
{ "found": true/false, "code": "...", "used": true/false, "usedBySession": "...或null", "usedAt": "...或null", "channel": "...", "batch": "...", "createdAt": "..." }
```

**B2 批次导出**:`GET /api/admin/codes?batch=xxx&format=csv` 导出该批次全部码(列与生成时的 CSV 一致,追加 used/usedAt 两列)。`format` 缺省时返回 JSON 数组。

**B3 每日统计**(上线后看漏斗用):`GET /api/admin/stats?days=7` 返回按天聚合:

```json
{ "days": [ { "date": "2026-07-12", "sessions": 0, "reports": 0, "oneTimeRedeems": 0, "sharedRedeems": 0 } ] }
```

日期按北京时间切天;`days` 参数 1~30,缺省 7。实现方式自定(允许基于现有表的 created_at 聚合查询),响应结构不得偏离。

---

## 任务组 C — 共享码每日名额上限(防"码泄漏当天无限白嫖")

**C1 环境变量**:`SHARED_DAILY_LIMIT`,正整数;未设置或为 0 表示不限(默认行为不变)。

**C2 数据表**(特批一):

```sql
create table if not exists shared_redemptions (
  id bigint generated always as identity primary key,
  window_start timestamptz not null,
  session_id text not null,
  created_at timestamptz not null default now()
);
create index if not exists idx_shared_redemptions_window on shared_redemptions (window_start);
```

内存实现语义对齐。`supabase/schema.sql` 同步追加。

**C3 核销逻辑**:`redeemSharedAccessCode` 中,verify 通过后:按**码所属窗口**(含宽限期内核销的旧码,计入旧窗口)统计 `shared_redemptions` 行数;达到上限则拒绝,`/api/redeem` 返回 403 与专属文案(附录 X-3,逐字);未达上限则先插入记录再 markSessionPaid。

**C4 单测**:①上限 2,第 1、2 次成功,第 3 次返回 X-3 文案;②未配置上限时不受限;③宽限期核销计入旧窗口的计数;④health 与统计不受影响。

---

## 任务组 D — 部署与环境配置

**D1 Netlify 环境变量**:核对以下变量全部配置(值由操作者提供,见附录 M):`SUPABASE_URL`、`SUPABASE_SERVICE_ROLE_KEY`、`ADMIN_SECRET`、`SHARED_ACCESS_CODE_SECRET`、`SHARED_ACCESS_WINDOW_MINUTES=1440`、`SHARED_DAILY_LIMIT`(可选)、`NEXT_PUBLIC_PAY_URL`、`NEXT_PUBLIC_SITE_URL`。

**D2 数据库就绪**:确认生产 Supabase 项目已执行最新 `supabase/schema.sql`(含 C2 新表)。Codex 无法直接操作 Supabase 控制台时,输出需要执行的 SQL 并提示操作者执行,收到确认后继续。

**D3 两段式部署**(解决 SITE_URL 先有鸡还是先有蛋):首次部署取得正式域名 → 将 `NEXT_PUBLIC_SITE_URL` 设为该域名 → 触发第二次构建部署。若操作者已绑定自定义域名,直接使用之。

**D4 构建验证**:`npm run build` 本地通过后再推送触发部署;部署日志无 A2 告警。

---

## 任务组 E — 生产冒烟验证

新建 `scripts/smoke-prod.mjs`,代码为附录 S-2 原文,逐字使用。运行方式:

```
SMOKE_BASE_URL=https://正式域名 ADMIN_SECRET=xxx node scripts/smoke-prod.mjs
```

该脚本在**正式网址**上完整走一遍:健康检查(storage 必须为 supabase)→ 建会话 → 结果页(付费按钮必须存在,不得出现兜底文案)→ 生成码 → 核销 → 定制题 → 报告(含缓存一致性复查)→ 一码一用复验 → 无效码报错 → 共享码核销。任何一项失败脚本退出码为 1。

---

## 任务组 R — 验收(逐项输出通过/失败,附原始输出)

- **R1** 第〇组四项检查当前全绿(热修未回退)。
- **R2** 生产 `GET /api/admin/health`:storage="supabase",四个 configured 字段全为 true。
- **R3** `smoke-prod.mjs` 对正式网址全项通过,**粘贴完整输出原文**。
- **R4** B1/B2/B3 三接口在生产可用,各贴一份样例响应(码值与 session 打码脱敏)。
- **R5** C4 四条单测全绿;生产将 `SHARED_DAILY_LIMIT` 临时设为 1 验证第 2 次核销返回 X-3 文案后,调回预定值并重新部署。
- **R6** 分享卡卡底 URL 显示正式域名(NEXT_PUBLIC_SITE_URL 生效)。
- **R7** `npx vitest run`、`npm run typecheck` 全绿;完成报告附:正式网址、Netlify 环境变量清单(值打码)、全部验收输出。

---
---

# 附录

## X-3 共享码名额已满文案(逐字)

「今天的限时名额已经用完啦——每天 18:00 会有新一批;不想等的话,购买兑换码可以立即解锁。」

## S-2 scripts/smoke-prod.mjs(整体新建,逐字使用)

```js
const BASE = process.env.SMOKE_BASE_URL?.replace(/\/$/, "");
const ADMIN = process.env.ADMIN_SECRET;
if (!BASE || !ADMIN) {
  console.error("用法: SMOKE_BASE_URL=https://域名 ADMIN_SECRET=xxx node scripts/smoke-prod.mjs");
  process.exit(1);
}

const ANSWERS = [0, 3, 0, 1, 1, 1, 2, 1, 0, 0, 3, 1, 3, 3, 2, 3];
const PREMIUM = [1, 1, 1, 1, 0, 0, 1];
let failed = 0;

function check(name, ok, detail = "") {
  console.log(`${ok ? "✅" : "❌"} ${name}${detail ? ` | ${detail}` : ""}`);
  if (!ok) failed += 1;
}

const sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms));

async function jsonReq(path, { method = "GET", body, admin = false } = {}) {
  const headers = { "content-type": "application/json" };
  if (admin) headers["x-admin-secret"] = ADMIN;
  const res = await fetch(`${BASE}${path}`, {
    method,
    headers,
    body: body ? JSON.stringify(body) : undefined,
  });
  const text = await res.text();
  let data = null;
  try { data = JSON.parse(text); } catch { data = null; }
  return { status: res.status, data, text };
}

async function main() {
  // 1. 健康检查:生产必须是 supabase
  const health = await jsonReq("/api/admin/health", { admin: true });
  check("health 接口可用", health.status === 200, `status=${health.status}`);
  check("存储为 supabase(严禁内存兜底)", health.data?.storage === "supabase", `storage=${health.data?.storage}`);
  check("PAY_URL 已配置", health.data?.payUrlConfigured === true);
  check("SITE_URL 已配置", health.data?.siteUrlConfigured === true);
  check("共享码密钥已配置", health.data?.sharedCodeConfigured === true);

  // 2. 建会话
  const session = await jsonReq("/api/session", { method: "POST", body: { answers: ANSWERS } });
  const sessionId = session.data?.sessionId;
  check("创建会话", session.status === 200 && Boolean(sessionId), `persona=${session.data?.persona?.title ?? "?"}`);
  if (!sessionId) return;

  // 3. 结果页:200 且付费入口存在(不得出现兜底文案)
  const resultPage = await fetch(`${BASE}/result/${sessionId}`);
  const resultHtml = await resultPage.text();
  check("结果页 200", resultPage.status === 200);
  check("付费入口存在(无兜底文案)", !resultHtml.includes("购买入口暂时维护中"));

  // 4. 生成一次性码并核销
  const created = await jsonReq("/api/admin/codes", { method: "POST", admin: true, body: { count: 1, channel: "smoke", batch: "smoke" } });
  const code = created.data?.codes?.[0]?.code;
  check("生成一次性码", created.status === 200 && Boolean(code));
  if (!code) return;
  await sleep(1200);
  const redeem = await jsonReq("/api/redeem", { method: "POST", body: { code, sessionId } });
  check("一次性码核销", redeem.status === 200 && redeem.data?.mode === "one_time");

  // 5. 定制题与报告
  const premium = await jsonReq("/api/session/premium", { method: "POST", body: { sessionId, premiumAnswers: PREMIUM } });
  check("提交定制题", premium.status === 200 && premium.data?.ok === true);
  const report1 = await jsonReq("/api/report", { method: "POST", body: { sessionId } });
  check("报告生成", report1.status === 200 && report1.text.length > 800, `${report1.text.length}字`);
  check("报告含综合判断", report1.text.includes("综合判断"));
  const report2 = await jsonReq("/api/report", { method: "POST", body: { sessionId } });
  check("报告缓存一致(种子稳定)", report2.text === report1.text);

  // 6. 一码一用:同码用于新会话必须失败
  const session2 = await jsonReq("/api/session", { method: "POST", body: { answers: ANSWERS } });
  const sessionId2 = session2.data?.sessionId;
  await sleep(1200);
  const reuse = await jsonReq("/api/redeem", { method: "POST", body: { code, sessionId: sessionId2 } });
  check("一码一用(复用被拒)", reuse.status === 400);

  // 7. 无效码报错
  await sleep(1200);
  const invalid = await jsonReq("/api/redeem", { method: "POST", body: { code: "AAAABBBBCCCC", sessionId: sessionId2 } });
  check("无效码返回400", invalid.status === 400, invalid.data?.error ?? "");

  // 8. 共享码核销
  const shared = await jsonReq("/api/admin/shared-code", { admin: true });
  const sharedCode = shared.data?.code ?? shared.data?.sharedCode;
  check("读取当前共享码", shared.status === 200 && Boolean(sharedCode));
  if (sharedCode && sessionId2) {
    await sleep(1200);
    const sharedRedeem = await jsonReq("/api/redeem", { method: "POST", body: { code: sharedCode, sessionId: sessionId2 } });
    check("共享码核销", sharedRedeem.status === 200 && sharedRedeem.data?.mode === "shared");
  }

  console.log(failed === 0 ? "\n全部通过 ✅" : `\n${failed} 项失败 ❌`);
  process.exitCode = failed === 0 ? 0 : 1;
}

main().catch((err) => {
  console.error("冒烟脚本异常:", err);
  process.exit(1);
});
```

## M 给操作者(人类)的配套清单(Codex 依赖这些值,缺则等待)

1. **生成两个密钥**(终端执行两次,分别用作 ADMIN_SECRET 与 SHARED_ACCESS_CODE_SECRET):`openssl rand -base64 24`。Windows 无 openssl 时可用 PowerShell:`[Convert]::ToBase64String((1..24 | ForEach-Object { Get-Random -Maximum 256 }))`。
2. **Supabase 生产项目**:新建项目 → SQL Editor 执行仓库 `supabase/schema.sql`(含本轮新表)→ 取 Project URL 与 service_role key。
3. **发卡平台**:上架 9.9 元商品(商品说明模板沿用生成码时的 deliveryText 风格),取得商品页链接,即 `NEXT_PUBLIC_PAY_URL`。
4. **域名**:可先用 Netlify 分配的子域名跑通,再绑自定义域名(绑定后记得改 NEXT_PUBLIC_SITE_URL 并重新部署)。
5. **上线后每天一眼**:`/api/admin/stats?days=7` 看漏斗(会话数→报告数→核销构成);共享码搞活动的当天盯 sharedRedeems,异常放大立即调小 SHARED_DAILY_LIMIT。
